-- Migration 21: restore draft timestamps as first-class schema fields

DEFINE FIELD IF NOT EXISTS created ON TABLE draft TYPE option<datetime> DEFAULT time::now();
DEFINE FIELD IF NOT EXISTS updated ON TABLE draft TYPE option<datetime> DEFAULT time::now();
